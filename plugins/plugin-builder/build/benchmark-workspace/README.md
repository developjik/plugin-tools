# Plugin Builder Benchmark Workspace

This is a small workspace for Plugin Eval benchmark runs.

Use `./tmp-marketplace` as the marketplace root for generated artifacts. The benchmark harness provisions the local plugin under `./plugins/plugin-builder`.
