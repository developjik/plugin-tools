#!/usr/bin/env node
'use strict';

const [maj] = process.versions.node.split('.').map(Number);
if (maj < 20) {
  process.stderr.write('plugin-builder requires Node >= 20\n');
  process.exit(78);
}

require('../build/core/cli.js').main(process.argv.slice(2));
